[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shared"
version = "0.1.0"
description = "Shared interfaces and models for the job tracker"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
